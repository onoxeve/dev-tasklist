<% if logged_in? %>
  <div class="row">
    <%= render 'tasks/form', task: @task %>
    <div class="col-xs-8">
      <%= render 'tasks/tasks', tasks: @tasks, user: @user %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Write like a ninjax</h1>
      <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>